table:
  name: vendors
  schema: public
array_relationships:
  - name: transactions
    using:
      foreign_key_constraint_on:
        column: vendor_id
        table:
          name: transactions
          schema: public
